let vendedor =prompt ("Insira o nome do vendedor");
let nivel =prompt ("Insira o nivel");
let venda= prompt ("Insira o valor da venda");

if(
    nivel == 1 

) {

if (
    venda > 200000){}
    console.log(venda * 0.2);

if (
    venda > 50000){}
    console.log(venda * 0.35);

    if (
        venda > 80000){}
        alert(`O vendedor ${vendedor}, nivel 1 foi promovido ao nivel 2 `);

        if (
            venda < 5000){}
            alert(`O vendedor ${vendedor}, nivel 1 foi promovido ao nivel 2 `);

            if (
                venda < 5000){}
                alert(`O vendedor ${vendedor}, nivel 1 está em risco de demissão `);} 

                if(
                    nivel == 2 
                
                ) {
                
                if (
                    venda > 90000){}
                    console.log(venda * 0.25);
                
                if (
                    venda > 120000){}
                    console.log(venda * 0.4);
                
                    if (
                        venda > 200000){}
                        alert(`O vendedor ${vendedor}, nivel 2 foi promovido ao nivel 3 `);
                
                        if (
                            venda < 5000){}
                            alert(`O vendedor ${vendedor}, nivel 2 esta em risco de virar nivel 1 `);
                
                           } 


                          
   
if(
    nivel == 3 

) {

if (
    venda > 200100){}
    console.log(venda * 0.3);

if (
    venda > 250000){}
    console.log(venda * 045.);

    if (
        venda > 500000){}
        alert(`O vendedor ${vendedor}, nivel 3 viro donoo `);

        if (
            venda < 5000){}
            alert(`O vendedor ${vendedor}, nivel 1 foi promovido ao nivel 2 `);

            if (
                venda < 5000){}
                alert(`O vendedor ${vendedor}, nivel 1 está em risco de demissão `);} 


